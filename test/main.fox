import fox.std.util -> u

func main() -> void{
    pi = 3.14
    radius = 10

    is_large = u.IntChangeDouble(radius) > pi and radius >= 5

    if (is_large == 1) {
        truncated = u.DoubleChangeInt(pi) + radius
        print(truncated)
    }
}